import { StudyPlan, Note } from '../types';

// Empty functions ready for backend integration
export const generateMockPlan = (formData: any): StudyPlan | null => {
  // This will be replaced with actual API call
  return null;
};

export const generateMockNotes = (fileName?: string): Note[] => {
  // This will be replaced with actual API call
  return [];
};